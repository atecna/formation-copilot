function convertKgtoGrams(kg) {
    return kg * 1000;
}

// Use fetch to connect to pokeapi 
// returns all first 151 pokemon names
// then map the data to return an array of pokemon names
function getPokemon() {}