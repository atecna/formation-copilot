/**
 * Create a React button component that is fully accessible. This button should:
 * - Use appropriate ARIA roles, such as 'button'.
 * - Be navigable by keyboard, i.e., accessible using the Tab key and activatable with Enter or Space key.
 * - Have a descriptive label accessible to screen readers.
 * - Manage 'focus' state for enhanced visibility.
 * - Support various sizes and variants (e.g., primary, secondary).
 * - Include 'aria-pressed' attributes for toggle buttons indicating active/inactive states.
 * - Incorporate PropTypes for type checking.
 * - Use CSS-in-JS for dynamic styling based on props.
 */